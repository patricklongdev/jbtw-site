{
  "name": "jbtw-site",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "node scripts/sync-content.mjs && astro dev",
    "build": "node scripts/sync-content.mjs && node scripts/generate-redirects.mjs && NODE_OPTIONS=--max-old-space-size=4096 astro build && pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.1.0"
  }
}
